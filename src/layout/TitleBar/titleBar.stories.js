import React from 'react';
import TitleBar from '.';

export default {
  title: 'UI组件/TitleBar',
  component: TitleBar,
};

export const Default = () => {
  return <TitleBar name="神原秋人" status="online" />;
};
