import React from "react";
import ContactCard from ".";

export default {
  title: "UI组件/ContactCard",
  component: ContactCard,
};

export const Default = () => {
  return <ContactCard>默认</ContactCard>;
};
